# Backend API base URL. Only needed for production (or when not using Vite proxy).
# In dev, leave unset to use the proxy target in vite.config.js (localhost:4000).
# Example for production (e.g. Vercel): https://your-backend.onrender.com/api
# VITE_API_URL=
